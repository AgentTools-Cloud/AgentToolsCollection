# --- Upstream OpenAI-compatible endpoint (only used by GET /v1/models) ---
UPSTREAM_BASE_URL=http://127.0.0.1:8080
UPSTREAM_API_KEY=sk-REPLACE_ME

# --- x402 self-description ----------------------------------------------
# Kept for /.well-known/x402 metadata. No paid routes are gated by this
# anymore (paid stack retired 2026-05-25), so the address can stay empty.
X402_PAY_TO=0xC445aa2AA0FA68db67Cd22fc04867773941f9CdF
X402_NETWORK=eip155:8453
X402_PRICE_USD=0.001
X402_FACILITATOR_URL=https://x402.org/facilitator

# --- Server -------------------------------------------------------------
HOST=127.0.0.1
PORT=9100
ALLOWED_MODELS=Qwen/Qwen3.6-35B-A3B

# --- Directory site (agent-tools.cloud) ---------------------------------
AGENT_TOOLS_DB_PATH=/opt/mcpserver/data/agent-tools.db
