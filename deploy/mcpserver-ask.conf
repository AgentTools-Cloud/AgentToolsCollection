[Service]
# Listed after the original /opt/mcpserver/.env; intentionally overrides only
# the nonsecret Ask policy. Existing project safety backend is explicitly shared.
EnvironmentFile=/etc/agent-tools/ask.env